apiVersion: v1
kind: Service
metadata:
  name: {{ template "im-access-identitymanager-frontend.fullname" . }}
  labels:
    app: {{ template "im-access-identitymanager-frontend.name" . }}
    chart: {{ template "im-access-identitymanager-frontend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "im-access-identitymanager-frontend.name" . }}
    release: {{ .Release.Name }}
