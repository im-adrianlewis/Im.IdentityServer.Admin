apiVersion: v1
kind: Service
metadata:
  name: {{ template "imaccessgraphportal.fullname" . }}
  labels:
    app: {{ template "imaccessgraphportal.name" . }}
    chart: {{ template "imaccessgraphportal.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "imaccessgraphportal.name" . }}
    release: {{ .Release.Name }}
